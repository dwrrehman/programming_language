
join push
join d
join e
join f
    join push
    join D
    join decl root
    join pop
    join param
join decl init
join pop

join push
join i
join d
join push
join x
join decl init
join pop
join param

join decl init
join def x
join pop
id id id char
