
join push
join d
join e
join f
    join push
    join D
    join decl root
    join pop
    join param
join decl init
join pop

join push
join m
join a
join c
join r
join o
join decl init
join pop

join push
join h
join e
join l
join l
join o
join decl init
join pop

join push
join (
join f
join push
join x
join decl init
join pop
join param
join )
join decl init
join def
    join
        x x
join pop
join macro

join (f join hello char)
(f hello)

