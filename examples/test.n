join (define push)
join (define pop)
join (define ((lazy ((x) (_))) (_)))
join (define ((((first) (_)) ((second) (_))) (_)))

(push)
(push)
(define ((my define ((signature) (name) (_)) ((T) (_)) ((definition) (lazy (T)) (_))) (_)))


(define ((hello there) (_)))

(define ((g) (lazy hello there) (_)))
(define ((f) (lazy name) (_)))

(my define hi hello there g)
(my define hh name f)
(my define hn hello there g)
 
(pop) (pop)
