join (define ((lazy ((x) (_))) (_)))
join (define push)
join (define pop)
join (define (( define ((signature) (name) (_))   ((type) (_))   ((definition) (lazy type) (_)) ) (_)))

join (define ((hello there) (_)))

join (define ((g) (hello there) (_)))

join (define ((f) (lazy name) (_)))

join (define htfs (hello there) (g))
join (define ((((first) (_)) ((second) (_))) (_)) (_) second)
hello there
